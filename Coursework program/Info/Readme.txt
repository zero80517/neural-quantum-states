To control weights evolution without dependency of the seed of the random generators, use:

	seed = ...
	ground.randomizer.seed(seed)

after module loading to choose the seed of the used random generators.